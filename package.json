{
  "name": "@eabasguliyev/sba-common",
  "version": "1.0.7",
  "description": "",
  "scripts": {
    "clean": "npx del-cli build --force",
    "build": "npm run clean && npx tsc",
    "pub": "git add . && git commit -m 'updates' && npm version patch && npm run build && npm publish && git push origin main"
  },
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "del-cli": "^5.0.0",
    "typescript": "^4.9.3"
  }
}
